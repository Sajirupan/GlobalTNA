# URL of your running Express backend.
# In development this is optional — Next.js rewrites /api/* to localhost:5000.
# In production (Vercel + Render/Railway) set this to your deployed backend URL.
NEXT_PUBLIC_API_URL=http://localhost:5000
